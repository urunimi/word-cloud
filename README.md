# word-cloud

* 의존성 설치

```sh
> python3 -m venv .venv
> source .venv/bin/activate
> pip install --upgrade pip
> pip install -r requirements.txt
```

* 실행

```sh
> source .venv/bin/activate
> python elena.py
```
